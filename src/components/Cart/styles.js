import styled from 'styled-components';

export const CartIcon = styled.div`
  display: block;
  position: absolute;
  align-items: center;
  right: 1rem;
  transform: translate(-100%, 60%);
  cursor: pointer;
`;
